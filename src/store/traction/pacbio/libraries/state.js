const state = {
  // Array of tagSetChoice objects with id and text (name) fields
  tagSetChoices: [],
  // Arrays of tagChoice objects with id and text (group_id) fields indexed by tagSetId
  tagChoices: {},
  libraries: [],
}

export default state
