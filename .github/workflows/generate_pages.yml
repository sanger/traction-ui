name: Deploy JSDoc to GitHub Pages

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'jsdoc-gh-pages'
  workflow_dispatch:

permissions:
  pages: write      # Allow writing to the GitHub Pages
  id-token: write   # Allow OIDC token to be issued

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup node
        uses: actions/setup-node@v4
        with:
            node-version-file: '.nvmrc'
            cache: 'npm'
      - name: Install dependencides
        run: >-
            npm ci --legacy-peer-deps
      - name: Run jsdoc
        run: |
            npm run jsdoc
            sed -i '/<h1 class="page-title">Home<\/h1>/d' docs/index.html
            sed -i 's|<img src="/public/traction-logo.svg" alt="logo">||' docs/index.html

      - name: Upload artifact to GitHub Pages
        uses: actions/upload-pages-artifact@v1
        with:
          path: docs  # Path to the folder containing the Yard documentation (default is 'doc')

  deploy:
    runs-on: ubuntu-latest
    needs: build  # The deploy job will only run if the build job is successful

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1